 
 evenent strcuture
  name: this.names[this.rnd(0, this.names.length - 1)],
            start: first,
            end: second,
            color: this.colors[this.rnd(0, this.colors.length - 1)],
            timed: !allDay,



clicking event open popup choose date title category (visite medical etc...)
cretate update delete event
deplacer  un evenent       